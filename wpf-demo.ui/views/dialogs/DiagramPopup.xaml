<Window x:Class="wpf.ui.views.dialogs.DiagramPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:wpf.ui.views.dialogs"
        xmlns:vm="clr-namespace:wpf.ui.viewmodels.popup"
        WindowStyle="{Binding WindowStyle}"
        WindowStartupLocation="Manual"
        Deactivated="Window_Deactivated"
        SizeToContent="WidthAndHeight"
        Loaded="Window_Loaded"
        ResizeMode="NoResize">
    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto">
        <ContentControl Content="{Binding DiagramViewModel}">
        </ContentControl>
    </ScrollViewer>
</Window>
